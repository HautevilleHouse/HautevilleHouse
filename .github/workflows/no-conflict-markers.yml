name: No conflict markers

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Reject unresolved merge markers
        shell: bash
        run: ./scripts/forbid-conflict-markers.sh
